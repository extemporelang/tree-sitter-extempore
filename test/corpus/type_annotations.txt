================================================================================
basic type annotation
================================================================================

(lambda (x:i64) x)

--------------------------------------------------------------------------------

(program
  (list
    (symbol)
    (list
      (symbol))
    (symbol)))

================================================================================
pointer type annotation
================================================================================

(lambda (buf:i8*) buf)

--------------------------------------------------------------------------------

(program
  (list
    (symbol)
    (list
      (symbol))
    (symbol)))

================================================================================
closure type annotation
================================================================================

(lambda (f:[i64,i64]*) (f 5))

--------------------------------------------------------------------------------

(program
  (list
    (symbol)
    (list
      (typed_identifier))
    (list
      (symbol)
      (number))))

================================================================================
tuple type in let
================================================================================

(let ((p:<double,double>* (alloc)))
  p)

--------------------------------------------------------------------------------

(program
  (list
    (symbol)
    (list
      (list
        (typed_identifier)
        (list
          (symbol))))
    (symbol)))

================================================================================
array type annotation
================================================================================

(let ((a:|4,float|* (alloc)))
  a)

--------------------------------------------------------------------------------

(program
  (list
    (symbol)
    (list
      (list
        (typed_identifier)
        (list
          (symbol))))
    (symbol)))

================================================================================
vector type annotation
================================================================================

(let ((v:/4,float/* (alloc)))
  v)

--------------------------------------------------------------------------------

(program
  (list
    (symbol)
    (list
      (list
        (typed_identifier)
        (list
          (symbol))))
    (symbol)))

================================================================================
generic type variable
================================================================================

(bind-func identity:[!a,!a]*
  (lambda (x) x))

--------------------------------------------------------------------------------

(program
  (list
    (symbol)
    (typed_identifier)
    (list
      (symbol)
      (list
        (symbol))
      (symbol))))

================================================================================
typed literal
================================================================================

(* 5:i64 3.0:f)

--------------------------------------------------------------------------------

(program
  (list
    (symbol)
    (number)
    (number)))

================================================================================
multiple typed args
================================================================================

(lambda (x:i64 y:i32 z:double) (+ x y))

--------------------------------------------------------------------------------

(program
  (list
    (symbol)
    (list
      (symbol)
      (symbol)
      (symbol))
    (list
      (symbol)
      (symbol)
      (symbol))))
